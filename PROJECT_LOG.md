# PROJECT LOG (변경 이력 · 작업자 명단 · 다음 할 일)

> 이 문서는 사람이 읽는 **작업 로그**입니다.
> Codex가 매 세션 자동으로 읽는 지침은 저장소 루트의 **`AGENTS.md`**에 있습니다.
> Codex는 작업 전 이 문서를 읽고, 작업 후 이 문서를 아래 규칙에 따라 갱신합니다.

---

## 1. 버전 관리 규칙 (Naming)

**버전 형식:** `vMAJOR.MINOR.PATCH` (예: `v1.3.2`)

| 자리 | 이름 | 올리는 경우 | 예시 |
|------|------|-------------|------|
| MAJOR | 대규모/호환성 파괴 | 구조 전체 변경, 이전 결과물과 호환 안 됨 | `v1.0.0 → v2.0.0` |
| MINOR | 기능 추가 | 새 기능·새 파일·새 모듈 추가 (기존은 유지) | `v1.2.0 → v1.3.0` |
| PATCH | 소규모 수정 | 버그 수정, 오타, 리팩터링, 문서 보완 | `v1.3.1 → v1.3.2` |

**엔트리 ID (변경 이력용 고유번호):** `YYYY-MM-DD-NN`
→ 날짜 + 그날의 순번(2자리). 예: `2026-07-06-01`, `2026-07-06-02`

**스냅샷/산출물 파일 네이밍(필요 시):** `<프로젝트약칭>_<모듈>_v<버전>.<확장자>`
→ 예: `4WD_encoder_v0.2.0.txt`, `perception_pipe_v1.1.0.py`
→ 스냅샷을 따로 저장할 때도 **이전 버전 파일은 지우지 않고 그대로 둔다.**

---

## 2. 현재 상태 (Snapshot — 이 섹션만 최신값으로 덮어쓰기)

- **현재 버전:** `v0.1.1`
- **최종 업데이트:** 2026-07-06 (KST)
- **최종 작업자:** `@AI` (작업자 명단 등록)
- **한 줄 요약:** 공동 작업자 7명(김준수·신정하·이유창·최인호·최인식·허준영·양승민) 명단 등록. `AGENTS.md`/`PROJECT_LOG.md` 2파일 구조로 분리.

---

## 3. 작업자 명단 (Collaborators)

> 새 작업자가 합류하면 이 표에 **추가**한다(기존 행 삭제 금지). 핸들(`@`)은 변경 이력에서 작업자를 식별하는 용도.

| 핸들 | 이름 | 역할 | 비고 |
|------|------|------|------|
| `@junsu` | 김준수 | (역할 미정) | |
| `@jeongha` | 신정하 | (역할 미정) | |
| `@yuchang` | 이유창 | (역할 미정) | |
| `@inho` | 최인호 | (역할 미정) | |
| `@insik` | 최인식 | (역할 미정) | |
| `@junyoung` | 허준영 | (역할 미정) | |
| `@seungmin` | 양승민 | (역할 미정) | |
| `@AI` | AI 어시스턴트 | 실제 작성/수정 담당 | 사람 대신 코드·문서 작성 |

*(역할 칸은 팀에서 정해지는 대로 채워 넣으세요.)*

---

## 4. 다음 할 일 (TODO / Next Actions)

> 완료 시 `[x]` 체크(삭제하지 말 것). 담당자는 `@핸들`로 지정.

- [x] 작업자 명단(3번 표)에 실제 팀원 정보 채우기 — `@AI` (2026-07-06 완료, 7명 등록)
- [x] 공통 프롬프트를 `AGENTS.md`(지침) / `PROJECT_LOG.md`(로그) 2개 파일로 분리 — `@AI` (2026-07-06 완료)
- [ ] 두 파일을 GitHub 저장소 루트에 커밋하고, `new` 브랜치가 아니라 `main`에도 반영하기 — 팀 전체
- [ ] 각 작업자 역할(제어/인식/하드웨어/문서/통합 등) 확정해 명단 표에 채우기 — 팀 전체
- [ ] 첫 실제 코딩 작업 요청 시 이 로그에 엔트리가 규칙대로 잘 추가되는지 검증 — `@AI`
- [ ] 프로젝트 약칭/모듈 명명 규칙 팀 합의 (스냅샷 파일명용) — `@seungmin`

---

## 5. 변경 이력 (Changelog) — ⛔ 삭제 금지 / 최신 항목이 맨 위

<!-- 새 엔트리는 바로 이 줄 아래에 추가하세요. 기존 엔트리는 건드리지 마세요. -->

### `v0.1.1` — 2026-07-06 (KST) · 작업자: `@AI` · ID: `2026-07-06-02`
- **변경 요약:** 공동 작업자 7명 명단 등록 + 문서를 `AGENTS.md`/`PROJECT_LOG.md` 2파일 구조로 분리.
- **상세:**
  - `3. 작업자 명단`에 김준수(`@junsu`), 신정하(`@jeongha`), 이유창(`@yuchang`), 최인호(`@inho`), 최인식(`@insik`), 허준영(`@junyoung`), 양승민(`@seungmin`) 추가.
  - Codex가 세션마다 자동으로 읽는 짧은 지침 파일 `AGENTS.md`를 신설하고, 실제 로그·명단·할일은 이 `PROJECT_LOG.md`에 남기도록 역할을 분리함 (32KiB 지침 용량 제한 대응 및 자동 인식을 위함).
  - `4. 다음 할 일`의 "명단 채우기" 항목 완료 체크, "파일 분리" 항목 완료 체크, "저장소에 커밋" 항목 신규 추가.
- **변경 파일:** `PROJECT_LOG.md` (수정), `AGENTS.md` (신규)
- **다음 할 일 반영:** 명단 채우기 `[x]` / 파일 분리 `[x]` / 저장소 커밋(main 반영) 신규 등록.
- **버전 근거:** 신규 기능 추가가 아닌 문서 정보 보완·구조 정리 → PATCH 상승 (`v0.1.0 → v0.1.1`).

### `v0.1.0` — 2026-07-06 (KST) · 작업자: `@AI` · ID: `2026-07-06-01`
- **변경 요약:** 공동작업용 공통 프롬프트 & 변경이력 로그 문서 최초 생성.
- **상세:**
  - AI 작업 지침, 버전 관리 규칙/네이밍 정의.
  - 현재 상태 스냅샷, 작업자 명단 템플릿, 다음 할 일, 변경이력, 엔트리 템플릿 구성.
  - "기록 삭제 금지 · 추가만 허용" 규칙 확정.
- **변경 파일:** `PROJECT_LOG.md` (신규, 최초에는 단일 파일이었음)
- **다음 할 일 반영:** 팀원 명단 채우기 / 첫 실제 작업 엔트리 검증 / 스냅샷 명명 규칙 합의 를 4장에 등록.
- **버전 근거:** 문서 최초 생성 → 초기 버전 `v0.1.0`.

<!-- ↑↑↑ 다음 작업부터는 위 새 엔트리 주석 바로 아래에 새 엔트리를 쌓아 올리세요. -->

---

## 6. 엔트리 작성 템플릿 (복사해서 5장 맨 위에 붙여넣기)

```markdown
### `v<버전>` — <YYYY-MM-DD HH:MM (KST)> · 작업자: `@<핸들>` · ID: `<YYYY-MM-DD-NN>`
- **변경 요약:** (한 줄로)
- **상세:**
  - (무엇을, 왜, 어떻게 바꿨는지)
- **변경 파일:** (파일명 / 신규·수정·삭제 표시)
- **다음 할 일 반영:** (완료 체크한 항목, 새로 추가한 할 일)
- **버전 근거:** (MAJOR/MINOR/PATCH 중 무엇을 왜 올렸는지)
```
