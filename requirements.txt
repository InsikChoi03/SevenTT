# ─── 주의 ────────────────────────────────────────────────────────────────────
# torch, torchvision, opencv-python은 JetPack에 내장된 CUDA 빌드를 사용합니다.
# pip으로 재설치하면 CUDA 지원이 빠진 CPU 버전이 설치되므로 절대 포함하지 마세요.
# 젯슨 전용 패키지 설치 방법은 docs/jetson_setup.md 를 참고하세요.
# ─────────────────────────────────────────────────────────────────────────────

numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
pyserial>=3.5
gdown>=4.7.1
