# AGENTS.md — Codex 공통 작업 지침

> 이 저장소에서 작업을 시작할 때 Codex가 세션마다 자동으로 읽는 지침 파일입니다.
> 실제 변경 이력·작업자 명단·다음 할 일은 이 파일이 아니라 **`PROJECT_LOG.md`**에 있습니다.
> Codex는 아래 순서를 매 작업마다 반드시 따르세요.

---

## 0. 기본 원칙

- 이 프로젝트는 **5명 이상의 공동 작업자**가 함께 사용합니다.
- **사람은 코드를 직접 작성하지 않습니다. 모든 코드/문서 작성·수정은 AI(Codex)가 수행합니다.**
- 작업 결과는 항상 사람이 나중에 읽고 이해할 수 있어야 합니다 (누가/언제/무엇을 했는지).

## 1. 작업 시작 시 (매번)

1. 저장소 루트의 `PROJECT_LOG.md`를 먼저 읽는다.
   - `2. 현재 상태`에서 최신 버전과 상황을 파악한다.
   - `4. 다음 할 일`에서 대기 중인 작업이 있는지 확인한다.
2. 사용자가 요청한 작업(코드 작성, 수정, 리팩터링 등)을 수행한다.

## 2. 작업 완료 시 (매번, 필수)

`PROJECT_LOG.md`를 다음 규칙에 따라 갱신한다:

1. **변경 이력(5장)에 새 엔트리를 맨 위에 추가한다.**
   - 기존 엔트리는 **절대 수정·삭제하지 않는다.** 정정이 필요해도 새 엔트리로 남긴다.
   - 형식은 `PROJECT_LOG.md`의 `6. 엔트리 작성 템플릿`을 그대로 사용한다.
   - 반드시 채울 항목: **버전 / 날짜·시간(KST) / 작업자 / 변경 요약 / 변경 파일 / 다음 할 일 반영**.
   - 작업자를 특정할 수 없으면 `@AI`로 표기한다.
2. **버전을 올린다** (`PROJECT_LOG.md`의 `1. 버전 관리 규칙` 참고):
   - MAJOR: 구조 전체 변경, 하위 호환 깨짐
   - MINOR: 새 기능/새 파일/새 모듈 추가
   - PATCH: 버그 수정, 문서 보완, 소규모 수정
3. **`2. 현재 상태` 섹션을 이번 작업 기준으로 덮어쓴다** (이 섹션만 덮어쓰기 허용).
4. **`4. 다음 할 일`을 갱신한다**: 완료 항목은 `[x]` 체크(삭제 금지), 새로 생긴 할 일은 담당자(`@핸들`)와 함께 추가한다.
5. 새 산출물 파일을 저장소에 남길 경우, 네이밍은 `<프로젝트약칭>_<모듈>_v<버전>.<확장자>` 형식을 따르고 **이전 버전 파일은 삭제하지 않는다.**

## 3. 절대 규칙

> ⚠️ **과거 기록(변경 이력, 작업자 명단, 완료된 할 일)은 절대 지우지 않는다. 항상 "새로 추가"만 한다.**

## 4. 참고

- 전체 규칙, 버전 네이밍 상세, 작업자 명단, 변경 이력 전문, 엔트리 템플릿은 모두 `PROJECT_LOG.md`에 있다.
- `PROJECT_LOG.md`가 없거나 찾을 수 없으면 작업을 중단하고 사용자에게 알린다.
